name: Institutional Heartbeat
on:
  # The schedule is removed, so this workflow will no longer run automatically
  workflow_dispatch:

jobs:
  check-system:
    runs-on: ubuntu-latest
    steps:
      - name: System Health Check
        run: |
          echo "Institutional Heartbeat is currently disabled."
