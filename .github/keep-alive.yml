name: Keep Render Alive
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Mission Critical Ping
        env:
          TARGET_URL: ${{ secrets.RENDER_URL }}
        run: |
          curl -sf "${TARGET_URL}/health"
